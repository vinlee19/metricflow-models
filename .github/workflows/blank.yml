# .github/workflows/validate-manifest.yml (只需这几行)
name: Validate Manifest
on:
  pull_request:
    paths:
      - 'manifest.json'

jobs:
  validate:
    uses: vinlee19/manifest-validator-action/.github/workflows/validate.yml@v1
